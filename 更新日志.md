# OriginLore 更新日志

## v2.0.1 (2026-09-02)

### 修复：玩家库存 `UNKNOWN` 回退实际从未生效

- `PlayerInventoryMixin` 已编译进 2.0.0 的 JAR，但从未注册到 `originlore.mixins.json`，因此 Fabric Mixin 根本不会加载它。v2.0.0 更新日志中"新增玩家库存和标准库存回退"这一条属于**未兑现的声明**，本次予以兑现。
- 后果是：模组直接调用 `player.getInventory().insertStack(...)` 或 `setStack(...)` 授予物品时，物品不会立即获得 `UNKNOWN` 来源元数据。`SimpleInventoryMixin` 与 `BlockEntityMixin` 只注入 `markDirty()`，且目标类都不是 `PlayerInventory`，无法覆盖这条路径。
- `/give`、战利品表、工作台、熔炉、切石、锻造和铁砧等已有专用入口不受影响。

### 修复：单个回调无法覆盖两个 `insertStack` 重载

- 原写法在一个 `@Inject` 中同时声明 `insertStack(ItemStack)` 与 `insertStack(int, ItemStack)`，但回调只接收 `(ItemStack, CallbackInfoReturnable)`，与两参数重载的签名不匹配。
- 由于 `defaultRequire: 1` 被另一个重载的成功注入所满足，这次失败**完全静默**：服务端正常启动，日志中没有任何 Mixin 错误或警告，只有两参数入口的物品拿不到组件。已将两个注入拆分为各自签名精确匹配的独立回调。
- 拆分后每个 `@Inject` 只有一个目标，`defaultRequire: 1` 因此变为有效约束：将来任一重载注入失败会立即报错，而不是再次退化为静默半覆盖。
- 已确认 `insertStack(ItemStack)` 内部委托给 `insertStack(int, ItemStack)`，故单次调用会触发两次组件应用。`ItemComponentManager` 在元数据版本、配置 revision 与来源均未变化时提前返回 `unchanged`，因此重复应用是幂等的，不会重新抽取随机变体。

### 验证

- 新增 GameTest `playerInventoryInsertionAppliesUnknownFallback`，分别覆盖 `insertStack(ItemStack)`、`insertStack(int, ItemStack)` 与 `setStack(int, ItemStack)` 三个入口，并同时断言基础规则（Lore）与 `UNKNOWN` 来源规则（`customName`）均已生效。拆分前该测试精确报出 `insertStack(int, ItemStack) did not apply the base Lore`；拆分后通过。
- Fabric Loader 0.19.2 与 0.19.3 下均为 JUnit 18 项、GameTest 12 项全部通过，无注入错误。
- 使用生产形态的 intermediary Minecraft jar 核验 refmap 中三个目标（`class_1661` 的 `method_7394`、`method_7367`、`method_5447`）描述符真实存在且完全一致。
- 静态扫描隔离整合包的 138 个模组 JAR：`fabric-carpet` 与 `collective` 分别以 `@Redirect` 触及 `insertStack` 的**调用点**（前者在 `insertStack(ItemStack)` 内部重定向对两参数重载的调用，后者在 `ItemEntity.playerTouch` 内部重定向对单参数重载的调用），与本次位于方法 HEAD 的 `@Inject` 不构成 redirector 冲突。
- **未重跑**：239 模组整合包服务端的实机启动验证仍为 2.0.0 时期的结果，2.0.1 未在真实整合包服务端实机运行过。

## v2.0.0 (2026-09-01)

### 服务端权威架构

- 配置升级到 schema v3，支持旧扁平配置迁移、原子写入、损坏回退和递增 revision。
- 管理 GUI 改为服务端快照与事务提交；权限等级低于 2 的客户端不能读取或修改配置。
- 网络协议 v2 使用 GZIP、有界分片、上传超时、版本冲突检查和有界响应。
- 普通玩家无需安装客户端模组，服务端生成的数据组件可由原版客户端使用。

### 来源、变体与组件

- 新增 14 种统一来源、具体战利品表/配方匹配和 `UNKNOWN` 回退。
- 新增基础、来源、加权随机变体三级合并；变体和随机数首次抽取后持久保存。
- 在 `minecraft:custom_data.originlore` 中保存来源身份、配置版本和原始组件补丁。
- 支持名称、Lore、稀有度、堆叠、耐久、防火、附魔、食物、状态效果、属性、工具、模型数据和 Tooltip 控制。
- 新增高级数据组件 JSON 编辑，并使用 Minecraft 组件 Codec 和 ItemStack 校验器拒绝无效组合。
- 删除受管理字段时恢复首次接管前的显式设置、显式移除或物品默认值。

### 生成与热刷新

- 来源入口覆盖 LootTable、方块/实体掉落、命令、工作台、熔炼、切石、锻造和铁砧。
- 熔炉、烟熏炉和高炉每次完成一件 OriginLore 物品后暂停，取出产物后再按最新权重开始下一件，避免单槽混入不同随机变体。
- 相同变体的熔炼产物忽略来源、配方和配置版本等 OriginLore bookkeeping 后可正常堆叠，不同变体仍保持隔离。
- 新增玩家库存和标准库存回退，无法识别的模组入口安全归入 `UNKNOWN`。
- 配置保存后增量刷新在线玩家、末影箱、装备、已加载容器、方块实体和物品实体。
- 玩家登录、区块/实体加载和库存变化时惰性刷新，不扫描未加载区块或离线存档。

### GUI 与质量

- 修复与 CAD Editor 0.1.0 共存时，点击“新增”或进入 JSON 编辑器会因其不兼容的原版多行文本 Mixin 导致客户端崩溃的问题；所有多行输入改用 OriginLore 自有控件，不修改第三方模组。
- 修复文本规则页底部样式按钮在高 GUI 缩放/矮窗口下与保存、取消按钮重叠的问题。
- 修复规则编辑器中“当前变体权重”标签与物品 ID 输入框相互重叠的问题，并为窄窗口的权重摘要和页面标签增加间距。
- 新增三层规则编辑、注册表 Tab 补全、Text JSON 样式、食物效果、附魔、属性、工具和高级组件编辑器。
- 编辑器使用事务副本，支持取消、断线保护、服务端校验错误和保存后自动更新版本。
- 选中变体后主编辑器直接显示权重输入框和实时概率；支持 `80` / `20` 或 `80%` / `20%`，权重可随时热修改。
- 修复下拉框和注册表补全弹层的文字透叠、行距过窄及屏幕边缘错位；弹层支持自动向上/向下展开、长文本裁切和滚轮选择。
- 来源类型在 GUI 中完整汉化，同时保留配置与网络协议使用的稳定英文枚举值。
- 新增中文使用手册，包含基础规则、来源、随机变体、食物效果、工具武器和高级组件的操作示例。
- Mixin 列表仅包含真实 Mixin，并固定使用 Minecraft 1.21.1 方法签名及生产 refmap。
- 新增配置、网络、合并、迁移、恢复、变体稳定性、Codec 和 GameTest 覆盖。
- 最低受支持 Fabric Loader 调整为 0.19.2；0.19.3 继续用于快速回归。

## v1.0.0 (2026-06-30)

- 初始未完成版本，使用扁平配置和 ItemStack 构造入口。
