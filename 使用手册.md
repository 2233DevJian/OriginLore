[English version](docs/en/USER_GUIDE.md) | [项目概览](README.zh-CN.md)

# OriginLore 中文使用手册

本手册对应 OriginLore 2.0.1、Minecraft 1.21.1 和 Fabric Loader 0.19.2。

## 1. 安装与权限

服务端必须安装：

- OriginLore。
- Fabric API。
- Fabric Loader 0.19.2 或更高的 1.21.1 兼容版本。

需要打开管理界面的客户端也要安装同一个 OriginLore JAR。普通玩家不需要安装 OriginLore 客户端，仍然可以正常看到和使用服务器生成的物品属性。

只有权限等级不低于 2 的 OP 可以读取和修改配置。进入世界后按 `O` 打开管理界面；按键可以在 Minecraft 的“选项 -> 控制”中修改。

## 2. 五分钟完成第一条 Lore

下面以甜浆果为例，让所有来源的甜浆果默认显示一条 Lore。

1. 进入单人世界或服务器，按 `O`。
2. 等右上角显示服务器配置版本，而不是“正在同步”或“无管理员权限”。
3. 点击“新增”。
4. 在“物品 ID”中输入 `minecraft:sweet_berries`。输入一部分后可按 `Tab` 补全。
5. 左侧保持选中“基础规则”。
6. 在“文本”页的 Lore 输入框中填写：`酸甜多汁的浆果，吃起来不错！`
7. 每行会成为一条独立 Lore；需要多行时直接换行。
8. 点击最下方“保存”。

保存成功后配置版本会自动增加，在线玩家和已加载对象中的受管理物品会开始刷新。以后重新加载的物品也会使用这条规则。

## 3. 先理解三层规则

编辑器左侧是当前物品的规则树，右侧编辑被选中的一层。蓝色文字表示当前正在编辑哪一层。

| 层级 | 用途 | 例子 |
| --- | --- | --- |
| 基础规则 | 该物品在所有情况下共享的默认值 | 所有甜浆果默认都是“酸甜多汁” |
| 来源规则 | 只在某一种来源匹配时覆盖基础规则 | 箱子里的浆果使用另一套食物属性 |
| 变体规则 | 在同一来源中首次生成时按权重抽取 | 箱子浆果分为新鲜、久放、腐烂 |

最终应用顺序是：

```text
基础规则 -> 匹配的来源规则 -> 首次抽到的变体规则
```

未填写的字段表示“继承”，不会把物品原值清空。一个已经由 OriginLore 接管的字段从配置中删除后，会恢复该物品首次被接管前保存的原始组件值。

## 4. 来源设置怎么用

### 添加来源

1. 在物品规则页点击左侧“+ 来源”。
2. 新来源默认是“未知来源”。
3. 保持该来源被选中，点击右上角“来源设置”。
4. 点击来源类型按钮，从下拉列表选择所需来源。
5. 需要精确匹配时填写战利品表 ID 或配方 ID；只想匹配来源大类时留空。
6. 点击“应用”返回物品规则页。

来源类型在中文界面中的含义：

| 中文显示 | 配置内部值 | 适用场景 |
| --- | --- | --- |
| 方块掉落 | `BLOCK_DROP` | 挖掘、采摘或破坏方块产生的物品 |
| 箱子战利品 | `CHEST_LOOT` | 结构、地牢等战利品表生成的物品 |
| 实体掉落 | `ENTITY_DROP` | 生物或其他实体死亡掉落 |
| 钓鱼 | `FISHING` | 钓鱼战利品 |
| 考古 | `ARCHAEOLOGY` | 可疑方块刷出的考古战利品 |
| 猪灵以物易物 | `BARTER` | 猪灵交易战利品 |
| 赠礼 | `GIFT` | 村民等系统赠送的物品 |
| 宝库 | `VAULT` | 试炼密室宝库产生的物品 |
| 命令生成 | `COMMAND` | `/give` 等命令生成的物品 |
| 工作台合成 | `CRAFTING` | 有形或无形工作台配方 |
| 熔炼 | `SMELTING` | 熔炉、高炉和烟熏炉配方 |
| 切石 | `CUTTING` | 切石机配方 |
| 锻造 | `SMITHING` | 锻造台转换、升级或纹饰 |
| 未知来源 | `UNKNOWN` | 旧物品或无法识别的模组自定义入口 |

战利品表 ID 示例：

```text
minecraft:chests/village/village_taiga_house
minecraft:chests/village/village_weaponsmith
```

配方 ID 示例：

```text
minecraft:iron_sword
```

如果具体 ID 在运行时无法取得，OriginLore 只会尝试同一来源大类中没有填写具体 ID 的规则，不会错误猜测成另一个来源。

## 5. 随机变体怎么用

变体必须属于某个来源，不能直接添加到基础规则下。

1. 先选中左侧的一个来源。
2. 点击“+ 变体”。
3. 选中新建的 `variant_1`。主编辑器右上方会出现“当前变体权重”输入框，也可以点击“来源设置”进入完整来源页面编辑。
4. 把变体 ID 改成稳定且不重复的名字，例如 `fresh`、`stored` 或 `rotten`。
5. 填写权重，例如 `80`、`20` 或 `80%`、`20%`。权重是相对概率，不要求总和为 100；左侧列表会显示每个变体的权重和实时概率。
6. 如果在“来源设置”页面编辑，点击“应用”返回主编辑器；主编辑器底部的“保存”才会真正提交。
7. 保持该变体被选中，在右侧各页面编辑它自己的 Lore、食物或属性。

例如三个变体权重分别是 6、3、1，则大约 60% 为 `fresh`、30% 为 `stored`、10% 为 `rotten`。至少一个变体的权重必须大于 0。

变体只在物品首次被接管时抽取一次。配置热修改、重启、复制、拆分、丢弃、区块重载或玩家重连都不会重新抽取；不同变体也不会错误堆叠。相同变体即使分别由熔炉和烟熏炉产出，只要实际组件相同也可以正常堆叠。

### 熔炉、烟熏炉和高炉的随机产出

OriginLore 为避免一个产物槽同时容纳不同变体，会在每件物品完成后暂停机器：当前物品完整烹饪结束并写入变体后，机器停止消耗燃料和推进进度，直到玩家或漏斗取走产物。取出后下一件物品重新按最新权重抽取；如果配置刚刚热修改，修改只影响这一次新的抽取，已经完成的产物不会改变。机器不会在烹饪中途因为“预测到下一个变体”而停止。

## 6. 完整的浆果示例

目标：野外采摘正常，箱子中可能出现新鲜、久放和腐烂浆果。

1. 为 `minecraft:sweet_berries` 的基础规则填写 Lore：`酸甜可口的浆果，吃起来不错！`
2. 新增来源并设置为“箱子战利品”。如果要覆盖所有箱子就不填具体战利品表；只覆盖某种箱子则填写对应 ID。
3. 在该来源下创建 `fresh`、`stored`、`rotten`，权重分别设为 6、3、1。
4. `fresh` 变体不填写食物字段，它会继承基础规则和原版食物组件。
5. 选中 `stored`，在 Lore 中填写“存放很久的浆果，吃着有股怪味...”。切到“食物”页，将营养值和饱和度调低。
6. 选中 `rotten`，填写腐烂 Lore，并设置更低的营养值和饱和度。
7. 在 `rotten` 的“食物”页点击“编辑食物状态效果”，添加恶心、饥饿等效果及触发概率。
8. 返回主编辑器后点击最下方“保存”，才会真正提交给服务器。

若野外采摘需要与默认规则不同，可再添加“方块掉落”来源规则；否则直接继承基础规则即可。

## 7. 工具和武器示例

以铁剑为例：

1. 新增或编辑 `minecraft:iron_sword`。
2. 添加“工作台合成”来源，配方 ID 可填 `minecraft:iron_sword`，用来描述玩家制作的铁剑。
3. 添加“箱子战利品”来源，战利品表可填 `minecraft:chests/village/village_weaponsmith`，用来描述铁匠铺箱子里的铁剑。
4. 在两个来源下分别编辑名称、Lore、耐久或属性修饰符。
5. 需要随机品质时，在对应来源下添加多个变体。

“数值”页的攻击伤害随机范围会在首次生成时抽取并保存。更复杂或需要指定槽位的属性请使用“能力 -> 属性修饰符”。

## 8. 四个主页面

### 文本

- “名称”：自定义物品名称，留空表示不接管名称。
- “Lore”：每行一条描述。
- `B`、`I`：粗体和斜体。
- “颜色”：循环选择颜色；“继承”表示不强制颜色。

当前简易样式会同时用于此层中通过表单编辑的名称和 Lore。需要不同颜色、悬停事件或更复杂文本时，使用“能力 -> 完整规则 JSON”编辑原版 Text JSON。

### 数值

- 最大堆叠数。
- 最大耐久和当前耐久损耗。
- 稀有度：可填 `COMMON`、`UNCOMMON`、`RARE`、`EPIC`，也可填 0 到 3。
- 自定义模型数据。
- 攻击伤害随机范围。

范围必须同时填写最小值和最大值，并且最小值不能大于最大值。

### 食物

- 营养值。
- 饱和度。
- 食用秒数。
- 是否可以在满饥饿值时食用。
- 概率状态效果。

“继承”表示该层不覆盖上层规则。要完整替换食物组件时，营养值与饱和度通常应一起填写。

### 能力

- 附魔与存储附魔。
- 属性修饰符。
- 工具挖掘规则。
- 食物状态效果。
- 高级数据组件。
- 完整规则 JSON。
- 防火、隐藏提示和隐藏附加提示的三态开关。

三态按钮按“继承 -> 是 -> 否 -> 继承”循环。这里的“继承”不是“否”，而是让上层规则或物品原值继续生效。

## 9. 子编辑器中的三个保存层次

这是最容易混淆的地方：

| 按钮 | 实际作用 |
| --- | --- |
| 保存条目 | 把当前附魔、属性、效果、工具规则或高级组件写入本地事务副本 |
| 应用到规则 | 把整个子编辑器副本交回当前基础/来源/变体规则 |
| 主编辑器最下方“保存” | 将完整物品配置提交给服务端、校验并写盘 |

因此，在属性编辑器中只点击“保存条目”后直接退出主编辑器，并不会修改服务器。正确顺序通常是“保存条目 -> 应用到规则 -> 主编辑器保存”。

任意页面点击“取消”都不会把该页面尚未应用的副本提交给上一层；在主编辑器点击“取消”会放弃整次物品编辑事务。

## 10. 补全和下拉操作

物品、战利品表、配方、附魔、属性、状态效果、方块和数据组件 ID 都使用服务端下发的注册表目录补全。

- 输入 ID 的开头后等待补全列表出现。
- `↑` / `↓`：切换候选项。
- 鼠标滚轮：浏览较长列表。
- `Tab` 或 `Enter`：采用当前候选项。
- `Escape`：关闭当前补全或选择列表。
- 鼠标点击：直接选择候选项。

下拉层会根据屏幕剩余空间自动向上或向下展开。弹层关闭后，下面的表单才会重新成为点击目标。

## 11. 属性、附魔与高级组件

### 属性修饰符

- 属性 ID 例：`minecraft:generic.attack_damage`。
- 修饰符 ID 必须是合法且稳定的命名空间 ID，例如 `originlore:rusty_sword_damage`。
- `add_value`：直接加固定值。
- `add_multiplied_base`：按基础值增加比例。
- `add_multiplied_total`：按最终值增加比例。
- 槽位决定修饰符何时生效，例如 `mainhand`、`offhand`、`head`。

### 附魔

普通附魔与存储附魔分别管理。附魔等级必须是整数；服务端会验证附魔 ID 是否存在。

### 高级数据组件

高级编辑器适合标准表单没有覆盖的原版或模组数据组件：

1. 输入组件 ID，例如 `minecraft:food`。
2. `SET` 模式填写该组件自身的 JSON 值。
3. `REMOVE` 模式表示明确移除该组件。
4. 点击“保存条目”，再点击“应用到规则”。

服务端会用当前 Minecraft 注册表和该组件的持久化 Codec 校验 JSON。不存在、不可持久化或无法解析的组件会拒绝保存，而不会把任意 JSON 写入物品。

## 12. 保存、热刷新和磁盘重载

GUI 的最终“保存”会执行以下过程：

1. 客户端提交自己打开编辑器时看到的配置版本。
2. 服务端重新验证权限、版本、物品 ID、组件和值。
3. 校验成功后使用临时文件和原子替换保存配置。
4. 配置版本递增并广播给在线管理员。
5. 在线背包、末影箱、装备、已加载容器、方块库存和物品实体开始增量刷新。

如果另一个管理员已经保存了新版本，当前旧编辑器会收到版本冲突提示。此时取消编辑，重新打开该物品后再修改，避免覆盖别人的配置。

磁盘配置位于：

```text
config/originlore/item_components.json
```

手工修改磁盘文件后，可由 OP 执行：

```text
/originlore reload
```

文件损坏或组件校验失败时，服务端会拒绝重载并继续使用上一份有效配置。

## 13. 常见提示

| 提示 | 原因与处理 |
| --- | --- |
| 无管理员权限 | 玩家不是权限等级 2 以上的 OP |
| 服务器不支持管理协议 | 服务器未安装 OriginLore，或客户端与服务端协议不兼容 |
| 未连接 | 已退出世界或网络连接断开，当前编辑器不能继续提交 |
| 版本冲突 | 其他管理员或磁盘重载已经产生新版本；重新打开编辑器 |
| 修改被拒绝 | 查看界面中的服务端校验错误，修正对应 ID、数值或 JSON |
| 找不到补全 | 先确认已经连接并同步完成；也可能是服务器注册表中不存在该 ID |

## 14. 重要行为边界

- OriginLore 不扫描未加载区块，也不直接修改离线世界存档。
- 来源不可追溯的旧物品属于“未知来源”，不会被猜成箱子或玩家合成。
- 已配置字段由 OriginLore 接管；未配置字段保留玩家、原版和其他模组的数据。
- 模组在 Java 中写死且没有标准数据组件表达的行为，不能仅靠高级组件伪造，需要单独兼容适配。
- 删除整个物品配置后，已经加载且保存过原始值的受管理物品会在刷新时恢复相应原始组件；未加载对象会在以后加载时惰性处理。
